import React from 'react';

import classes from './Registration.module.scss';

const RegistrationView = () => {
	return <section className={classes['container']}>View users</section>;
};

export default React.memo(RegistrationView);
